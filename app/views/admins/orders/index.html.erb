<p>注文履歴</p>
<% @orders.each do |order| %>
<table>
		<tr>
			<th>購入日時</th>
			<th>購入者</th>
			<th>注文個数</th>
			<th>注文ステータス</th>
		</tr>
		<tr>
			<td><%= l order.created_at %></td>
			<td><%= order.member.last_name %><%= order.member.fast_name %></td>
			<td><%= order.product.quantity %></td>
			<td><%= order.status_i18n %></td>
		</tr>
		<% end %>
</table>