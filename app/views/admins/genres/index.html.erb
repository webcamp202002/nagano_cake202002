<div>
	<h1>ジャンル追加•一覧画面</h1>
	<div>
		<%= form_for(@genre)  do |f| %>
		<p>ジャンル
			<%= f.label :name %>
			<%= f.text_field :name%>
		</p>
		<label>
			<%= f.radio_button :status, true %>有効
		</label>
		<label>
			<%= f.radio_button :status, false %>無効
		</label>
		<%= f.submit %>
		<% end %>
	</div>
	<table>
		<tr>
			<th>ジャンル</th>
			<th>状態</th>
			<th></th>
		</tr>
		<%if @genres.present? %>
		<% @genres.each do |genre| %>
		<tr>
			<td><%= genre.name %></td>
			<td><%= genre.status %></td>
			<td><%= link_to "編集する",edit_admins_genre_path(genre.id) %></td>
		</tr>
		<% end %>
		<% end %>
	</table>
</div>